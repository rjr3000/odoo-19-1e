# odoo-19-1e Railway deploy — thin layer on factory image (enterprise already baked).
# SSOT: odoo-19-images/odoo-19-e1 → evoluzion26/odoo-19-e1:latest
# EE modules live at /opt/odoo/enterprise in the base image. No zip, no release URL.

FROM evoluzion26/odoo-19-e1:latest

LABEL org.opencontainers.image.title="odoo-19-1e" \
      org.opencontainers.image.description="Railway Neon deploy on evoluzion26/odoo-19-e1 (baked enterprise)"
